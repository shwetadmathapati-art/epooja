# Copy this file to .env.local and fill the values to enable Supabase
# Vite exposes variables prefixed with VITE_ to the client bundle.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: admin override for local testing
VITE_ADMIN_EMAIL=

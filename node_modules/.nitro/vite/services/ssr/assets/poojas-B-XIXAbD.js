import { t as Button } from "./button-Ck9qVa6J.js";
import { t as supabase } from "./client-Drb7wB_Z.js";
import { t as PoojaCard } from "./cards-CeSu9jbx.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/poojas.tsx?tsr-split=component
var filters = [
	"all",
	"pooja",
	"homa",
	"consultation"
];
function PoojasPage() {
	const [filter, setFilter] = useState("all");
	const { data, isLoading } = useQuery({
		queryKey: ["poojas", "all"],
		queryFn: async () => {
			const { data, error } = await supabase.from("poojas").select("*").eq("active", true).order("category").order("base_price");
			if (error) throw error;
			return data;
		}
	});
	const list = (data ?? []).filter((p) => filter === "all" || p.category === filter);
	return /* @__PURE__ */ jsxs("div", {
		className: "mx-auto max-w-6xl px-4 py-16",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "label-mono",
				children: "Service catalogue"
			}),
			/* @__PURE__ */ jsx("h1", {
				className: "mt-2 text-5xl",
				children: "Poojas & Homas"
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-4 max-w-xl text-sm text-muted-foreground",
				children: "Every ritual is available as pooja-only or pooja-with-samagri. Prices below are the starting point; final amount depends on the priest you choose."
			}),
			/* @__PURE__ */ jsx("div", {
				className: "mt-8 flex flex-wrap gap-2",
				children: filters.map((f) => /* @__PURE__ */ jsx(Button, {
					size: "sm",
					variant: filter === f ? "ritual" : "quiet",
					onClick: () => setFilter(f),
					className: "capitalize",
					children: f
				}, f))
			}),
			isLoading ? /* @__PURE__ */ jsx("p", {
				className: "mt-10 text-sm text-muted-foreground",
				children: "Loading rituals…"
			}) : /* @__PURE__ */ jsx("div", {
				className: "mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3",
				children: list.map((p) => /* @__PURE__ */ jsx(PoojaCard, { pooja: p }, p.id))
			})
		]
	});
}
//#endregion
export { PoojasPage as component };
